name: timely
description: "A simple time registration app for PYME."
publish_to: "none"
version: 1.0.0+1
environment:
   sdk: ^3.10.0

dependencies:
   flutter:
      sdk: flutter

   cupertino_icons: ^1.0.8
   flutter_riverpod: ^3.0.3
   flutter_svg: ^2.2.3
   shared_preferences: ^2.5.3
   go_router: ^17.0.0
   uuid: ^4.5.2
   intl: ^0.20.2
   package_info_plus: ^8.1.2

   # Firebase
   firebase_core: ^3.6.0
   cloud_firestore: ^5.4.4
   flutter_native_splash: ^2.4.7

dev_dependencies:
   flutter_test:
      sdk: flutter

   flutter_lints: ^6.0.0

flutter:
   uses-material-design: true

   assets:
      - assets/images/
      - assets/mock/
